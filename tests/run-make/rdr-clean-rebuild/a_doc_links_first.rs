#![allow(dead_code)]

/// [`Alpha`] [`Beta`] [`Gamma`] [`Delta`] [`Epsilon`] [`Zeta`] [`Eta`] [`Theta`]
pub struct Api;

pub struct Alpha;
pub struct Beta;
pub struct Gamma;
pub struct Delta;
pub struct Epsilon;
pub struct Zeta;
pub struct Eta;
pub struct Theta;
